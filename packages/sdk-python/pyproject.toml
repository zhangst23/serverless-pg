[project]
name = "cloudpg"
version = "0.1.0"
description = "CloudPG Python SDK"
requires-python = ">=3.10"
dependencies = ["httpx>=0.27.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
